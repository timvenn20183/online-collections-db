Fields are a way to add custom information to your items. An example of this would be the video format for console games. e.g. ("PAL" or "NTSC"). A fields called "Video Format" would be created with options of "PAL" and "NTSC". You can then assign as many of the fields options and fields to the item. The first thing it so create the field and then allocation options to it.
